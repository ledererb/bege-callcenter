[build]
builder = "NIXPACKS"
buildCommand = "pip3 install -r thinkai-voice-agent/requirements.txt"

[deploy]
startCommand = "cd thinkai-voice-agent && bash start.sh start"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
